# Copy to .env.local and set the real API URL once Slim 4 backend is deployed.
# Until set, every API call falls back to local mock JSON — app stays fully usable.
VITE_API_BASE_URL=http://localhost:8080
