<!-- RootLayout.vue
     Shell that wraps every page: Navbar + AuthModal + DevRoleSwitcher.
     On mount: calls eventsStore.fetchAll() to load events from the API
     (falls back to mock JSON if the backend is not yet running).
     Handles login/logout and redirects to the correct dashboard by role.
-->
<template>
  <!-- TODO: implement -->
</template>
